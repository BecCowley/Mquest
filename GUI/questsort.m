%    function questsort - this sorts whatever it is given and returns the
%    INDEX of the sorted result - without changing the order of the
%    original.  this allows you to sort on one variable and then use that
%    index to sort all other associated variables, as well as the original.

function indexorder=questsort(tobesorted)

